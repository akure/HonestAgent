# Sprint Trace — M3 / Sprint 4

| Field | Value |
|---|---|
| Milestone | `M3` |
| Sprint | `Sprint 4` |
| Objective | Provide a real guarded upstream path and a testable provider adapter boundary. |
| Timestamp UTC | `2026-08-29 06:07:06` |
| Status | `complete` |

## Scope

Implemented `HA-008` through an optional OpenAI-compatible verifier provider adapter and `HA-010` through a guarded upstream chat-completion client. Live credentials remain optional and are not required for CI.

## Execution trace

| Order | Task | Action | Result | Evidence |
|---:|---|---|---|---|
| 1 | `HA-008` | Added structured provider adapter with injectable `httpx` client. | Complete | `honest_agent/core/providers.py` |
| 2 | `HA-008` | Added malformed-response contract failure. | Complete | `tests/test_m3_integrations.py` |
| 3 | `HA-010` | Added upstream client and optional environment configuration. | Complete | `honest_agent/interfaces/upstream.py` |
| 4 | `HA-010` | Connected proxy forwarding only after `PROCEED`. | Complete | `honest_agent/interfaces/proxy.py` |
| 5 | Validation | Ran full regression and mocked integration tests. | Complete | 28 passing tests |

## Failures discovered

No failing regression remained after implementation. The integration tests deliberately cover the negative path: paused requests produce zero upstream calls, and malformed provider output raises a contract error for the guard to fail closed.

## Decisions and trade-offs

The first real integration path is OpenAI-compatible because it gives customers a familiar HTTP contract while preserving provider choice. The local simulation remains the deterministic default so contributors can reproduce tests without credentials. Live streaming and provider-specific adapters are deferred until a design-partner workflow requires them.

## Validation

| Gate | Command or artifact | Result |
|---|---|---|
| Unit / integration | `python3 -m pytest -q` | **PASS — 28 tests** |
| Provider parsing | `tests/test_m3_integrations.py` | **PASS** |
| Malformed provider contract | `tests/test_m3_integrations.py` | **PASS** |
| Upstream safe forwarding | `tests/test_m3_integrations.py` | **PASS — one call** |
| Paused-action gate | `tests/test_m3_integrations.py` | **PASS — zero calls** |

## Git publication

| Commit | Message | Remote status |
|---|---|---|
| `{filled after commit}` | `feat: add guarded upstream passthrough` | Pending publication |

## Milestone decision

M3 / Sprint 4 is complete for the optional upstream and provider boundary. Publish the trace and changelog before starting M4. M4 should focus on pilot usability and evidence export rather than adding more providers speculatively.
