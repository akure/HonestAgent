# Agent instructions (HonestAgent)

This project is a **micro1 Agentic Workflows Hackathon** work-in-progress submission.

Cursor project skills live in `.cursor/skills/<skill-name>/SKILL.md`. Load **`micro1-hackathon-orchestrator` first**, then the gate skills it names. An always-on pointer is in `.cursor/rules/hackathon-submission.mdc`.

Do not treat root `SKILL.md` as the hackathon rubric; that file is the HonestAgent runtime guardrail for integrated agents.
